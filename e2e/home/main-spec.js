/* global require */
var angularRestTab = require('./restClient-po.js');
describe('home page', function() {
//to do as UI is changed
/*    var restTab = new angularRestTab();

    beforeEach(function() {
        browser.get('http://localhost:9000');
    });

    it('should get Firefly version', function() {
      restTab.setHost('localhost:61617');
      restTab.setCommand('api/version');
        restTab.submit();
        expect(restTab.getErrorMsg()).toEqual('Request Successful!');
    });*/
});
